<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleMathComponent">

    <Style TargetType="local:BoxRenderer" >
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:BoxRenderer">
                    <SwapChainPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="5" BorderBrush="ForestGreen">
                        <TextBox Text="This is the BoxRenderer comes from NuGet package."/>
                        <TextBox Text="This panel have a ForestGreen border with thickness=5 in NuGet package." />
                    </SwapChainPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
    
    
