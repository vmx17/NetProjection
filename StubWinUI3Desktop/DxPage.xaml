<Page
    x:Class="StubWinUI3Desktop.DxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StubWinUI3Desktop"
    xmlns:prj="using:SimpleMathComponent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel BorderThickness="10" BorderBrush="DarkMagenta" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <TextBlock Text="DarkMagenta frame is StackPanel's border in stub. The thickness is 10."/>
        <prj:BoxRenderer Width="1280" Height="768" BorderThickness="5" BorderBrush="Yellow"/>
        <TextBlock Text="Above, there should be 'BoxRenderer'(SwapChainPanel from WRC) with Yellow border. (Is the border meanless?)"/>
    </StackPanel>
</Page>
