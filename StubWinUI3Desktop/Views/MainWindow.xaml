<Window
    x:Class="StubWinUI3Desktop.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StubWinUI3Desktop.Views"
    xmlns:vm="using:StubWinUI3Desktop.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Activated="Window_Activated">

    <Frame x:Name="rootFrame" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Frame.DataContext>
            <vm:MainWindowViewmodel x:Name="viewModel"/>
        </Frame.DataContext>
        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <local:MainPage x:Name="mainPage" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        </Grid>
    </Frame>
</Window>
